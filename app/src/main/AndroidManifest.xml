<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tiendita">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Tiendita">
        <activity android:name=".ui.usuario.mapa.vistaGeneral"></activity>
        <activity android:name=".ui.producto.productoAgregar.productoAgregar" />
        <activity android:name=".ui.producto.productoIndividual.productoIndividual" />
        <activity android:name=".ui.usuario.pedido.pedidos" />
        <activity android:name=".ui.negocio.perfil.perfil" />
        <activity android:name=".ui.usuario.perfil.perfil" />
        <activity android:name=".ui.login.RecoverPassword" />
        <activity android:name=".ui.login.SignUp" />
        <activity android:name=".ui.login.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.Tiendita.NoActionBar" />
    </application>

</manifest>